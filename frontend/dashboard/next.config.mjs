/** @type {import('next').NextConfig} */
const nextConfig = {
    // Adicione esta linha:
    output: 'standalone',
  
    // ...outras configurações que você possa ter
  };

export default nextConfig;

// no seu arquivo next.config.mjs
